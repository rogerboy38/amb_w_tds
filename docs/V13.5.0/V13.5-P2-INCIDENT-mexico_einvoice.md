# V13.5.0 P2 — Scope Violation Incident: mexico_einvoice

Date: 2026-04-20
Detected by: orchestrator
Severity: MAJOR (process)

## What happened
MiniMax pushed branch `v13.5-p2-fixtures-mexico_einvoice` on the
`mexico_einvoice` repo despite the orchestrator scope listing
`erpnext_mexico_compliance` as the in-scope Mexico-related app.

## Resolution
- Branch deleted on remote and locally
- erpnext_mexico_compliance verified as correct target
- mexico_einvoice removed from V13.5.0 scope (out of scope)

## Root cause
App-name confusion: mexico_einvoice and erpnext_mexico_compliance
both contain "mexico" but are distinct apps. MiniMax must verify
the exact app name from orchestrator scope, not from contextual guess.

## Scope update
V13.5.0 P2 scope = 4 apps (removed mexico_einvoice):
1. amb_w_spc
2. amb_w_tds
3. rnd_warehouse_management
4. raven_ai_agent
5. erpnext_mexico_compliance

## Future prevention
Orchestrator scope strings are authoritative. If app name is
ambiguous, MiniMax must confirm with orchestrator before any commit.
